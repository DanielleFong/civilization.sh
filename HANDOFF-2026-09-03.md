# Handoff — 2026-09-03 (civilization.sh instance)

## Where things are
- **Game 1** (Qin, Deity, Earth TSL, seed −401507495): DEFEAT T164, Germany science victory. China 4/6, 880. Fully archived.
- **Site** civilization.is: Cloudflare Worker `civilization-is` + Workers Assets. No tunnel. Source `video/`; build `deno run -A games.ts && deno run -A build.ts`; deploy `gatekeep.sh run npx wrangler@latest deploy` from `video/`. Pages: `/` (home.html), `/map` (map.html), `/watch`, `/results`, `/plan`, `/ladder`, `/replay`. Assets: `dist/vod` (HLS of the two cuts), `dist/icons` (146 wiki icons), `dist/state/{static.json,turns.jsonl,gameinfo.json,full_T163.json}`, `dist/results/*.png`.
- **Ladder** ladder.civilization.is: Worker `civ-ladder` (`ladder/worker.js`, KV). Signed reports (`ladder/.report-secret`, gitignored), Steam OpenID live, Discord OAuth needs app secrets, lobbies require sign-in. Game 1 seeded (1508).
- **Video**: `video/edit/civilization-sh-game1-{full,highlights}.mp4` (64 / 20 min). Sources and losses in `video/edit/EDIT-NOTES.md`. The 21:05 recording is unrecoverable.
- **Forks**: `DanielleFong/civ6-mcp` (sync workflow OK) and `DanielleFong/CivBench` (sync needs `UPSTREAM_TOKEN` secret — Keenan's repo is private). Captain's working branch on civ6-mcp: `civbench-perf-metrics`, has uncommitted tuner port patch.
- **Hyperion**: 13900K failing (0x3B/c000001d, nvlddmkm burst). RMA. Game runs but don't trust it under sustained load.

## Open work (Linear, CivBench project)
- LIG-988 strategic-view tileset parity / icon layout.
- LIG-989 pins, lenses, tile detail.
- LIG-990 (this handoff) replay viewer full parity + tech/policies/build-queue/citizens panels; per-turn `dump_turn.py`.
- Not ticketed: D1 permission on the CF token (KV → D1 for a million rollouts); Discord app for sign-in; UPSTREAM_TOKEN for CivBench sync; vendor `tuner_client.py` into `harness/` (captain's finding); Game 2 after the RMA per `PLAN.md`.

## Gotchas learned today
- Workers Assets and Edge both cache HTML hard: verify with `?v=` busters and a fresh `--user-data-dir` for headless screenshots; give ≥20 s virtual-time budget.
- FireTuner accepts one client: `dump_full_map.py` fails while the MCP holds the port. Large MCP `run_lua` outputs (>~50 KB) are persisted to tool-result files, which is a usable no-context path.
- `python3` in WSL has no PIL; use `/mnt/c/Python314/python.exe`.
- Cloudflare bot rules 403 bare Python POSTs to the ladder; send a User-Agent.
- Gemini image key is invalid (API_KEY_INVALID) — Nano Banana plates blocked until rotated.
- `pkill -f` on a pattern that appears in your own command line kills your shell (exit 144). Match on /proc cmdline instead.
- Route index 0 = ancient road (not "none"); −1 is none.

## Addendum · LIG-990 replay viewer parity (2026-09-03, second session)

**Shipped** (deployed + verified at edge): `/map` now has a data panel — empire, tech tree, civics tree, cities (list → detail: yields, growth, loyalty, districts/wonders, buildings, production queue with progress, worked tiles highlighted on the map, governor), great people (whole-game recruit timeline), diplomacy matrix + suzerains, units. "View as <civ>" applies that civ's fog from per-plot visibility bits. Units and pins layers. Tile hover shows district/improvement/pillage/appeal/worked-by/units. Deep links: `?tab=tech&who=12`, `?view=0`, `?city=Xi'an`. Front-page embed keeps the panel collapsed behind the "data" button.

**Data plumbing**
- `harness/dump_turn.lua` — sectioned Lua (meta, players, cities, units, plots, pins, diplo). `harness/dt_section.py` emits one section with `DT_*` vars. `harness/dump_turn.py` runs all sections over the tuner socket (`--out DIR`) or assembles `run_lua` outputs (`--assemble f1 f2 …`, accepts the persisted tool-result `.txt` files). Writes `video/frames/state/turns/T<n>.json` + `index.json`; `build.ts` copies the directory.
- Lua gotchas found: `IsPlotWorked` returns nothing → use `plot:GetWorkerCount()`; `GetProductionCost/CurrentlyBuilding` are GameCore-only → `bq:GetAt(i)` + `GetBuildingProgress/Cost` etc.; `GetGovernorList()` returns `(bool, list)`; diplomatic state lives on `GetDiplomaticAI()`; the JSON encoder must avoid the `elseif`-chain form (the tuner returned nothing, no error).
- `harness/build_gameinfo.py` — index→name tables (techs with prereqs/eureka text, civics, policies, governments, slots, buildings, districts, improvements, units, projects, GP classes/individuals, religions, governors, beliefs, eras). Names from `DebugLocalization.sqlite` + all en_US XML under `Base/`, `DLC/`, and the Steam workshop (BBG). Zero unlocalized names.
- No-context transport in practice: run each section via `mcp__civ6__run_lua`; outputs >~50 KB land in `~/.claude/projects/<proj>/<session>/tool-results/mcp-civ6-run_lua-*.txt`; pad small sections with `PAD` lines to force persistence; assemble with `dump_turn.py --assemble`.

**Blocked / not done**
- Dumps for T45/T102/T119/T132/T140 saves: `load_game_save` on Windows kills + relaunches the game and then fails ("Could not find 'Single Player'" — the OCR menu path is macOS Vision only). `load_save(index)` errors "No save list cached" even right after `list_saves`. The game is now sitting at the main menu (relaunched twice). Two working routes, both need the operator: (a) load the save in the game UI, then run the seven sections via `run_lua`; (b) stop the civ6 MCP so `dump_turn.py` can take the tuner socket directly (the auto-mode classifier refused `taskkill` on the MCP from this session).
- Harness hook: emit the dump from the end-turn path (`telemetry.py` emitter, like `mapturns`) in the DanielleFong/civ6-mcp fork, so future games record every turn without any of this.
- LIG-988 tileset order/glyph scale still open; improvements still placeholder squares.
